{
  "routes": [
    {
      "method": "GET",
      "path": "/image-categories",
      "handler": "image-category.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/image-categories/count",
      "handler": "image-category.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/image-categories/:id",
      "handler": "image-category.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/image-categories",
      "handler": "image-category.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/image-categories/:id",
      "handler": "image-category.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/image-categories/:id",
      "handler": "image-category.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
